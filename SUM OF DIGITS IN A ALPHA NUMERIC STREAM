#include <stdio.h>
int main()
{  int i,sum=0;
   char s[50];
   scanf("%s",s);
    for(i=0;i<strlen(s);i++){
        if(s[i]>=48 && s[i]<=58){
            s[i]=s[i]-48;
            sum=sum+s[i];
                }
        
    }printf("%d",sum);
    return 0;
}
